//
// SEED DATA - The application starts with no data by default.
// Add contacts via the UI or import a CSV/JSON file from Settings.
//
import { Contact, FollowUp, Group, Interaction } from '../types';

export const INITIAL_GROUPS: Group[] = [];

export const INITIAL_CONTACTS: Contact[] = [];

export const INITIAL_INTERACTIONS: Interaction[] = [];

export const INITIAL_FOLLOWUPS: FollowUp[] = [];